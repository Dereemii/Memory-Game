import { GameContainer } from '../components/GameContainer'
import { Layout } from './Layout'

export const Game = () => {
  return (
    <Layout>
        <GameContainer isFlipped/>
    </Layout>
  )
}
